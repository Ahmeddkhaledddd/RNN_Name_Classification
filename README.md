# RNN_Name_Classification
A simple PyTorch RNN that merges input characters with hidden state to predict name categories. Includes data loading, tensor conversion, forward pass, and hidden-state initialization
